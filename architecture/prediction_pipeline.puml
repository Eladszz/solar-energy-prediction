@startuml
start
:Receive validated PV inputs;
:Retrieve Open-Meteo hourly weather;
:Sort and validate hourly timestamps;
:Calculate approximate POA irradiance (W/m²);
:Estimate cell temperature with NOCT;
:Calculate DC power (W -> kW);
:Apply system losses and AC clipping (kW);
:Treat each one-hour sample as kWh;
:Aggregate monthly and yearly kWh;
:Calculate flat-tariff value and payback;
stop
@enduml
